{% block log_in_widget %}
    {{ form_start(form) }}
    <div id="log_in">
        <div class="form-group">
            {{ form_label(form.username) }}
            {{ form_widget(form.username) }}
            <small>Dit is meestal uw emailadres</small>
        </div>
        <div class="form-group">
            {{ form_label(form.password) }}
            {{ form_widget(form.password) }}
        </div>
        <div class="form-group">
            {{ form_widget(form.login) }}
        </div>
        {{ form_widget(form) }}
    </div>
    {{ form_end(form) }}
{% endblock %}
